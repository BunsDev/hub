/** @jsxImportSource theme-ui **/
import styles from "./styles";

const LoadingSpinner = () => {
  return <div className="lds-dual-ring" sx={styles.loadingSpinner} />;
};

export default LoadingSpinner;
